<link href="/css/styl-portMUSICO.css" rel="stylesheet" type="text/css">
<script src="script/portifolio-musico.js"></script>

<title>Perfil MÃºsico</title>

<div class="body-container">
    <div class="trends">

    </div>
    <div class="profile-page">
        <div class="profile-info">
            <div class="profile-details foto">
                <img id="imagem" class="profile-photo" src="" alt="Imagem do Perfil" />

                <div class="profile-meta">
                    <h1 id="nome" name="nome" class="profile-name">

                    </h1>
                    <p id="email" name="email" class="profile-title">

                    </p>
                    <p id="opcoes" name="opcoes" class="profile-style">

                    </p>
                    <div class="profile-actions">
                        <button class="profile-button">Conectar</button>
                        <button class="profile-button"><a href="/attMUSICO">Editar Perfil</a></button>
                        <button class="profile-button">Compartilhar</button>
                    </div>
                </div>
            </div>
            <div class="profile-summary">
                <h2>Sobre</h2>
                <p id="descricao" name="descricao">

                </p>
            </div>
        </div>
        <div class="feed">
            <h2>Criar Postagem</h2>
            <form id="uploadForm" action="https://liebe4735.c44.integrator.host/posts/create" method="post"
                enctype="multipart/form-data">
                <label for="title">Title:</label><br>
                <input type="text" name="title" id="title"><br><br>

                <label for="description">Description:</label><br>
                <textarea name="description" id="description" rows="4"></textarea><br><br>

                <label for="video">Upload Video:</label><br>
                <input type="file" name="video" id="video" accept="video/*"><br><br>

                <label for="image">Upload Image:</label><br>
                <input type="file" name="image" id="image" accept="image/*"><br><br>

                <button type="submit">Create Post</button>
            </form>

            <script>
                const BASE_URL = document.getElementById('config').getAttribute('data-base-url');
                document.getElementById("uploadForm").addEventListener("submit", function (event) {
                    const videoInput = document.getElementById("video");
                    const imageInput = document.getElementById("image");

                    // Verificar se os arquivos foram selecionados
                    if (!videoInput.files.length && !imageInput.files.length) {
                        event.preventDefault();
                        alert("Por favor, envie pelo menos um vídeo ou uma imagem.");
                    }
                });
            </script>

            <div class="posts">

            </div>
        </div>
    </div>
</div>